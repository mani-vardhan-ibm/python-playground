apiVersion: v1
kind: Service
metadata:
  name: python-playground
  annotations:
    note: "Placeholder service – extend when an HTTP server app is added."
spec:
  selector:
    app: python-playground
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
